import { Grid } from '@mui/material';
import { styled } from '@mui/material/styles';


export const ContentWrappers = styled(Grid)(() => ({
    display: 'flex',
    justifyContent: 'center',
    marginTop: '2rem'
}))